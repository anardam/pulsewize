---
phase: 05-growth-tracking-and-dashboard
plan: "06"
type: execute
wave: 4
depends_on:
  - "05-04"
  - "05-05"
files_modified:
  - src/components/dashboard/GrowthChart.tsx
  - src/app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements:
  - GROW-03
  - GROW-04

must_haves:
  truths:
    - "GrowthChart renders an AreaChart with engagement, score, and estimatedReach metrics over time"
    - "GrowthChart handles empty data (fewer than 2 points) with an encouraging empty state"
    - "GrowthChart is marked 'use client' and imported from dashboard page via dynamic({ ssr: false })"
    - "Dashboard page displays the growth chart for the most-analyzed profile below the activity feed"
    - "Purple gradient (#a855f7) applied to chart data lines per project accent color convention"
  artifacts:
    - path: "src/components/dashboard/GrowthChart.tsx"
      provides: "Recharts AreaChart wrapper with dark theme and 3 metrics"
      exports: ["GrowthChart"]
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Updated dashboard with growth chart wired in (D-03)"
      contains: "GrowthChart"
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/components/dashboard/GrowthChart.tsx"
      via: "dynamic(() => import(...), { ssr: false })"
      pattern: "dynamic.*GrowthChart.*ssr.*false"
    - from: "src/components/dashboard/GrowthChart.tsx"
      to: "recharts"
      via: "AreaChart, Area, ResponsiveContainer"
      pattern: "from \"recharts\""
---

<objective>
Build the GrowthChart component using Recharts (engagement, score, estimatedReach) and wire it into the dashboard page for the most-analyzed profile — delivering the visual trend charts for GROW-03 and GROW-04.

Purpose: Charts are the core "growth tracking" feature. They transform flat report data into visible progress over time.
Output: GrowthChart.tsx (Recharts AreaChart with 3 metrics) + dashboard page updated to display the chart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-growth-tracking-and-dashboard/05-RESEARCH.md
@src/lib/chart-data.ts
@src/lib/types.ts
@.planning/phases/05-growth-tracking-and-dashboard/05-04-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/chart-data.ts (created in 05-01):
```typescript
export interface ChartPoint {
  date: string;           // "Jan 15"
  engagement: number;     // engagementStats.rate
  score: number;          // profileScore.overall
  estimatedReach: number; // engagementStats.estimatedReach (per D-07)
}

export function buildChartData(rows: Array<{ report_data: AnalysisReport; analyzed_at: string }>): ChartPoint[];
```

Recharts 3.8.1 imports (use client components only):
```typescript
import {
  AreaChart, Area, XAxis, YAxis, CartesianGrid,
  Tooltip, ResponsiveContainer, Legend,
} from "recharts";
```

IMPORTANT: Recharts 3.x — Tooltip must appear BEFORE Legend in JSX order.
Use "monotone" for area type (smooth curves).
Purple accent: #a855f7 (project convention from CONTEXT.md specifics).
Dark theme tooltip:
```typescript
contentStyle={{ background: "#111118", border: "1px solid rgba(255,255,255,0.08)", borderRadius: 8 }}
```

SSR guard: dashboard page must import GrowthChart via:
```typescript
import dynamic from "next/dynamic";
const GrowthChart = dynamic(() => import("@/components/dashboard/GrowthChart").then(m => ({ default: m.GrowthChart })), { ssr: false });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build GrowthChart component</name>
  <files>src/components/dashboard/GrowthChart.tsx</files>
  <behavior>
    - Renders without crashing when data is []
    - Shows empty state text "Analyze the same profile again to track growth" when data.length < 2
    - When data.length >= 2, renders AreaChart with engagement, score, and estimatedReach Areas
    - Component has "use client" directive at top
    - ResponsiveContainer wraps the chart with width="100%" height={220}
    - X-axis uses dataKey="date", Y-axis is present
    - Three Area components: engagement (stroke #a855f7), score (stroke #7c3aed), estimatedReach (stroke #6d28d9)
    - Gradient fill applied to engagement area
  </behavior>
  <action>
Create `src/components/dashboard/GrowthChart.tsx`:

```typescript
"use client";

import {
  AreaChart,
  Area,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
} from "recharts";
import type { ChartPoint } from "@/lib/chart-data";

interface Props {
  data: ChartPoint[];
  title?: string;
}

export function GrowthChart({ data, title = "Growth Trend" }: Props) {
  if (data.length < 2) {
    return (
      <div className="bg-[#111118] border border-white/[0.08] rounded-xl p-6">
        <p className="text-sm font-medium mb-2">{title}</p>
        <div className="flex items-center justify-center h-[220px] text-sm text-muted-foreground">
          Analyze the same profile again to track growth
        </div>
      </div>
    );
  }

  return (
    <div className="bg-[#111118] border border-white/[0.08] rounded-xl p-6">
      <p className="text-sm font-medium mb-4">{title}</p>
      <ResponsiveContainer width="100%" height={220}>
        <AreaChart data={data} margin={{ top: 4, right: 4, bottom: 0, left: 0 }}>
          <defs>
            <linearGradient id="engagementGrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="5%" stopColor="#a855f7" stopOpacity={0.25} />
              <stop offset="95%" stopColor="#a855f7" stopOpacity={0} />
            </linearGradient>
            <linearGradient id="scoreGrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="5%" stopColor="#7c3aed" stopOpacity={0.2} />
              <stop offset="95%" stopColor="#7c3aed" stopOpacity={0} />
            </linearGradient>
            <linearGradient id="reachGrad" x1="0" y1="0" x2="0" y2="1">
              <stop offset="5%" stopColor="#6d28d9" stopOpacity={0.15} />
              <stop offset="95%" stopColor="#6d28d9" stopOpacity={0} />
            </linearGradient>
          </defs>
          <CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,0.06)" />
          <XAxis
            dataKey="date"
            stroke="#666"
            tick={{ fill: "#666", fontSize: 11 }}
          />
          <YAxis
            stroke="#666"
            tick={{ fill: "#666", fontSize: 11 }}
            width={38}
          />
          <Tooltip
            contentStyle={{
              background: "#111118",
              border: "1px solid rgba(255,255,255,0.08)",
              borderRadius: 8,
            }}
            labelStyle={{ color: "#ededed" }}
            itemStyle={{ color: "#a855f7" }}
          />
          <Area
            type="monotone"
            dataKey="engagement"
            name="Engagement %"
            stroke="#a855f7"
            strokeWidth={2}
            fill="url(#engagementGrad)"
          />
          <Area
            type="monotone"
            dataKey="score"
            name="Profile Score"
            stroke="#7c3aed"
            strokeWidth={2}
            fill="url(#scoreGrad)"
          />
          <Area
            type="monotone"
            dataKey="estimatedReach"
            name="Est. Reach"
            stroke="#6d28d9"
            strokeWidth={2}
            fill="url(#reachGrad)"
          />
        </AreaChart>
      </ResponsiveContainer>
    </div>
  );
}
```

After implementation, run the RED component tests from 05-01 (src/__tests__/components/GrowthChart.test.tsx) — they should go GREEN.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "GrowthChart" | head -5</automated>
  </verify>
  <done>GrowthChart.tsx has "use client" directive, imports from recharts, renders empty state when data.length < 2, and renders AreaChart with 3 Areas (engagement, score, estimatedReach) when data.length >= 2. No TypeScript errors. GrowthChart component tests from 05-01 pass GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GrowthChart into dashboard page</name>
  <files>src/app/(dashboard)/dashboard/page.tsx</files>
  <action>
Update `src/app/(dashboard)/dashboard/page.tsx` to add the growth chart (per D-03):

1. Add dynamic import at the top of the file (after other imports):
```typescript
import dynamic from "next/dynamic";
import { buildChartData } from "@/lib/chart-data";

const GrowthChart = dynamic(
  () => import("@/components/dashboard/GrowthChart").then((m) => ({ default: m.GrowthChart })),
  { ssr: false }
);
```

2. Add a data fetch for the most-analyzed profile's trend data. After the existing Supabase queries, add:
```typescript
// Find the most-analyzed profile handle
const { data: topHandleRows } = await supabase
  .from("reports")
  .select("username")
  .eq("user_id", user.id)
  .eq("report_type", "analysis")
  .order("analyzed_at", { ascending: false })
  .limit(30);

// Find the handle with the most occurrences
const handleCounts = (topHandleRows ?? []).reduce<Record<string, number>>((acc, r) => {
  acc[r.username] = (acc[r.username] ?? 0) + 1;
  return acc;
}, {});
const topHandle = Object.entries(handleCounts).sort((a, b) => b[1] - a[1])[0]?.[0] ?? null;

// Fetch chart data for the top handle
let chartPoints: import("@/lib/chart-data").ChartPoint[] = [];
if (topHandle) {
  const { data: chartRows } = await supabase
    .from("reports")
    .select("report_data, analyzed_at")
    .eq("user_id", user.id)
    .eq("username", topHandle)
    .eq("report_type", "analysis")
    .order("analyzed_at", { ascending: true })
    .limit(30);
  chartPoints = buildChartData(
    (chartRows ?? []) as Array<{ report_data: import("@/lib/types").AnalysisReport; analyzed_at: string }>
  );
}
```

3. Replace the `<div id="growth-chart-slot">` placeholder from Plan 04 Task 2 with:
```jsx
{topHandle && (
  <div className="mt-8">
    <h2 className="text-base font-semibold mb-4">Growth trend — @{topHandle}</h2>
    <GrowthChart data={chartPoints} title={`@${topHandle} — engagement, score & reach`} />
  </div>
)}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard page imports GrowthChart via dynamic({ ssr: false }). Chart is shown below the activity feed for the most-analyzed profile handle. If no reports exist, the chart section is not rendered. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `grep "use client" src/components/dashboard/GrowthChart.tsx` returns a match
- `grep "ssr: false" src/app/(dashboard)/dashboard/page.tsx` returns a match
- `grep "AreaChart" src/components/dashboard/GrowthChart.tsx` returns a match
- `grep "buildChartData" src/app/(dashboard)/dashboard/page.tsx` returns a match
- `grep "estimatedReach" src/components/dashboard/GrowthChart.tsx` returns a match
</verification>

<success_criteria>
GrowthChart renders engagement, score, and estimatedReach trends with Recharts. Dashboard page shows chart for the most-analyzed profile via SSR-safe dynamic import. Empty state shown when fewer than 2 data points exist.
</success_criteria>

<output>
After completion, create `.planning/phases/05-growth-tracking-and-dashboard/05-06-SUMMARY.md`
</output>
