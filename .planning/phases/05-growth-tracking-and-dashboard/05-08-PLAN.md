---
phase: 05-growth-tracking-and-dashboard
plan: "08"
type: execute
wave: 5
depends_on:
  - "05-06"
  - "05-07"
files_modified: []
autonomous: false
requirements:
  - GROW-01
  - GROW-02
  - GROW-03
  - GROW-04
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Dashboard shows real stats and activity feed after running at least one analysis"
    - "Reports page shows card grid with working platform filter and search"
    - "Growth chart renders with data when the same profile has been analyzed more than once"
    - "Settings profile edit saves display name and avatar"
    - "CompetitorComparison saved-reports toggle is visible and usable"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Live dashboard"
      contains: "StatsCards"
    - path: "src/app/(dashboard)/reports/page.tsx"
      provides: "Reports list with filters"
      contains: "ReportCard"
    - path: "src/components/dashboard/GrowthChart.tsx"
      provides: "Growth trend chart"
      contains: "AreaChart"
    - path: "src/components/settings/ProfileEditSection.tsx"
      provides: "Profile editing UI"
      contains: "display_name"
  key_links: []
---

<objective>
Human verification of all Phase 5 features end-to-end in a live browser session.

Purpose: Visual and interactive features (charts, card grids, avatar upload, filters) require a human to verify correctness that automated tests cannot fully capture.
Output: Confirmed phase acceptance or actionable list of issues to address.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-growth-tracking-and-dashboard/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build check</name>
  <files></files>
  <action>
Run the complete test suite to confirm all tests pass before human verification:

```bash
npx vitest run
```

Then verify the build compiles without errors:

```bash
npm run build 2>&1 | tail -20
```

If any tests fail: fix the failing test first before proceeding to the checkpoint.
If the build fails: fix all TypeScript/import errors before proceeding.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5 && npm run build 2>&1 | grep -E "error|Error" | head -10</automated>
  </verify>
  <done>npx vitest run exits 0. npm run build completes without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 5 implementation:
- Dashboard page with live stats (total analyses, platforms, plan, monthly usage), recent activity feed, growth trend chart
- Reports page with card grid, platform filter, search, and pagination
- Settings page extended with display name and avatar editing
- CompetitorComparison extended with "From saved reports" toggle
  </what-built>
  <how-to-verify>
Run `npm run dev` and open http://localhost:3000

1. **Dashboard (/dashboard)**
   - Log in and navigate to /dashboard
   - Verify 4 stats cards show (total analyses, platforms used, plan, monthly usage)
   - Verify recent activity feed lists past analyses with platform, handle, and date
   - Verify the "Analyze a profile" CTA button is visible
   - If you have analyzed the same profile more than once: verify the growth trend chart appears below the feed
   - If only one analysis exists: verify the chart section shows "Analyze again to track growth" empty state

2. **Reports (/reports)**
   - Navigate to /reports
   - Verify report cards appear in a grid (handle, platform, date, score badge visible)
   - Test the platform filter dropdown — select "instagram" and verify the list updates
   - Test the search box — type a handle and verify the list filters
   - If you have more than 12 reports: test pagination prev/next buttons

3. **Settings (/settings)**
   - Navigate to /settings
   - Verify the new "Profile" section appears above the Account section
   - Enter a new display name and click "Save changes" — verify "Saved" confirmation appears
   - (Optional) Upload a 100KB JPG avatar — verify it uploads and appears in the avatar circle
   - Verify BillingSection and Danger zone sections are still present below

4. **Competitor Comparison**
   - Navigate to /analyze, run an analysis, open the Compare tab
   - Verify the "Enter handles" and "From saved reports" toggle buttons are visible
   - Click "From saved reports" — verify the saved report checklist appears
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 5 complete, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All automated tests pass and build succeeds before the human checkpoint.
</verification>

<success_criteria>
Human verifier has approved all 4 verification areas or all reported issues have been fixed and re-verified. Phase 5 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-growth-tracking-and-dashboard/05-08-SUMMARY.md`
</output>
