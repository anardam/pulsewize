---
phase: 04-ai-enhancement
plan: "06"
type: execute
wave: 3
depends_on:
  - "04-04"
  - "04-05"
files_modified:
  - src/components/ReportDashboard.tsx
  - src/components/CompetitorComparison.tsx
  - src/components/ContentCalendar.tsx
  - src/components/HashtagStrategy.tsx
  - src/app/analyze/page.tsx
autonomous: true
requirements:
  - AI-03
  - AI-04
  - AI-05

must_haves:
  truths:
    - "User can click 'Compare Competitors' from a report and enter 1-2 additional handles to compare"
    - "Competitor comparison renders a side-by-side metrics table with gradient accent on the leader column"
    - "User can click 'Generate Calendar' from a report and see a 7-column weekly grid (Mon-Sun, 4 weeks)"
    - "User can click 'Hashtag Strategy' from a report and see 4 categorized tag groups with avoid list"
    - "All 3 new features are accessible from the existing ReportDashboard without a page reload"
  artifacts:
    - path: "src/components/CompetitorComparison.tsx"
      provides: "Side-by-side comparison table + narrative"
      contains: "CompetitorComparisonReport"
    - path: "src/components/ContentCalendar.tsx"
      provides: "7-column weekly grid calendar"
      contains: "ContentCalendarReport"
    - path: "src/components/HashtagStrategy.tsx"
      provides: "Categorized hashtag display"
      contains: "HashtagStrategyReport"
  key_links:
    - from: "src/components/ReportDashboard.tsx"
      to: "src/components/CompetitorComparison.tsx"
      via: "rendered in activeTab === 'compare' branch"
      pattern: "CompetitorComparison"
    - from: "src/components/ContentCalendar.tsx"
      to: "/api/calendar"
      via: "fetch POST /api/calendar"
      pattern: "api/calendar"
    - from: "src/components/HashtagStrategy.tsx"
      to: "/api/hashtags"
      via: "fetch POST /api/hashtags"
      pattern: "api/hashtags"
---

<objective>
Build 3 new UI components (CompetitorComparison, ContentCalendar, HashtagStrategy) and wire them into ReportDashboard as additional tabs. Each component fetches from its respective API endpoint and renders the structured report.

Purpose: Makes the Phase 4 AI features visible and usable to end users. The report page becomes the hub for all advanced analysis features.
Output: 3 new components + updated ReportDashboard with tab navigation for compare/calendar/hashtags.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-ai-enhancement/04-CONTEXT.md
@src/lib/types.ts
@src/components/ReportDashboard.tsx
</context>

<interfaces>
<!-- Design system from CLAUDE.md — all Tailwind, dark theme only -->
```
Background: bg-gray-900/50, bg-gray-800/50, bg-[#0a0a0f]
Accent gradient: from-purple-600 via-pink-600 to-orange-500
Text: text-white, text-gray-400, text-gray-500 for muted
Success: text-green-400, Error: text-red-400
Opacity: bg-purple-500/10, bg-purple-500/20
Mobile-first: sm: breakpoints
Cards: .card-glow class for hover glow
Gradient text: .gradient-text class
```

<!-- From src/lib/types.ts -->
```typescript
interface CompetitorComparisonReport { platform, analyzedAt, profiles, metricsTable: CompetitorMetricRow[], narrative, opportunities }
interface ContentCalendarReport { platform, username, niche, generatedAt, weeks: CalendarWeek[] }
interface HashtagStrategyReport { platform, username, niche, generatedAt, categories: HashtagCategory[], avoidList, weeklyRotationPlan, captionMixFormula }
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build CompetitorComparison, ContentCalendar, HashtagStrategy components</name>
  <files>
    src/components/CompetitorComparison.tsx,
    src/components/ContentCalendar.tsx,
    src/components/HashtagStrategy.tsx
  </files>
  <read_first>
    - src/components/ReportDashboard.tsx — understand existing component patterns, Tailwind classes, dark theme usage
    - src/lib/types.ts — CompetitorComparisonReport, ContentCalendarReport, HashtagStrategyReport exact shapes
    - .planning/phases/04-ai-enhancement/04-CONTEXT.md — D-08 (table + narrative), D-10 (weekly grid), D-11 (full entry detail)
  </read_first>
  <action>
Create src/components/CompetitorComparison.tsx:

"use client" component. Props: `{ platform: string; currentUsername: string; currentReport: AnalysisReport }`.

State: `state: "input" | "loading" | "result"`, `usernames: string` (comma-separated input), `report: CompetitorComparisonReport | null`, `error: string | null`.

Input state: textarea for "Enter 1-2 competitor handles (comma-separated)", "Compare" button. Show current profile username pre-filled as first handle.

On Compare: fetch POST /api/compare with `{ platform, usernames: [currentUsername, ...parsedHandles] }`, show loading spinner.

Result state:
- Metrics table (D-08): `<table>` with columns for each profile username as header. Leader column gets `bg-purple-500/20 text-purple-300 font-semibold` highlight. Metric rows: Followers, Engagement Rate, Posts, Verification.
- Narrative: `<p className="text-gray-300 leading-relaxed">` for each paragraph
- Opportunities: bulleted list with `text-green-400` bullets

Tailwind: dark theme (bg-gray-800/50 cards), accent gradient on section headers, mobile responsive.

Create src/components/ContentCalendar.tsx:

"use client" component. Props: `{ platform: string; username: string; report: AnalysisReport }`.

State: `state: "idle" | "loading" | "result"`, `calendar: ContentCalendarReport | null`, `error: string | null`.

Idle state: "Generate 30-Day Content Calendar" button with gradient styling.

On Generate: fetch POST /api/calendar with `{ platform, username, analysisReport: report }`.

Result state (D-10 weekly grid):
- 4 `CalendarWeek` sections, each with header "Week N"
- 7-column grid (`grid-cols-7`) with day names (Mon-Sun) as column headers
- Each cell: contentType badge, contentIdea (truncated to 2 lines), optimalPostingTime in small text
- Click to expand: modal or inline expansion showing full CalendarEntry (captionDraft, hashtags, mediaSuggestion, engagementPrediction)
- Export button: copies all calendar data to clipboard as formatted text

Create src/components/HashtagStrategy.tsx:

"use client" component. Props: `{ platform: string; username: string; report: AnalysisReport }`.

State: `state: "idle" | "loading" | "result"`, `strategy: HashtagStrategyReport | null`, `error: string | null`.

Idle state: "Get Hashtag Strategy" button.

On Generate: fetch POST /api/hashtags with `{ platform, username, analysisReport: report }`.

Result state (D-13):
- 4 category cards (`grid-cols-1 sm:grid-cols-2`), each showing: category name, tags as chips with `bg-purple-500/10 text-purple-300 border border-purple-500/20 rounded-full px-2 py-0.5 text-xs` styling, estimatedReach, competitionLevel badge (green=low, yellow=medium, red=high)
- Avoid list: red-tagged chips `bg-red-500/10 text-red-400`
- captionMixFormula: highlighted formula box
- weeklyRotationPlan: text paragraph

All 3 components: `export default function ComponentName` (no React.FC), `interface Props` at top, all Tailwind, no CSS modules.
  </action>
  <verify>
    <automated>cd /Users/Mahesh.Sangawar/personal/anardam/InstaAnalyse && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 3 component files exist
    - `grep "use client" src/components/CompetitorComparison.tsx src/components/ContentCalendar.tsx src/components/HashtagStrategy.tsx` returns 3 lines
    - `grep "api/compare\|api/calendar\|api/hashtags" src/components/CompetitorComparison.tsx src/components/ContentCalendar.tsx src/components/HashtagStrategy.tsx` returns 3 lines (one each)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>3 new UI components created. Each fetches from its API and renders structured report. TypeScript clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire 3 new components into ReportDashboard as tabs</name>
  <files>src/components/ReportDashboard.tsx</files>
  <read_first>
    - src/components/ReportDashboard.tsx — read entire file; understand current tab structure and report prop shape
    - src/components/CompetitorComparison.tsx — props interface
    - src/components/ContentCalendar.tsx — props interface
    - src/components/HashtagStrategy.tsx — props interface
  </read_first>
  <action>
Read src/components/ReportDashboard.tsx in full. Make targeted edits:

**Edit 1 — Add imports** at top of file (after existing imports):
```typescript
import CompetitorComparison from "@/components/CompetitorComparison";
import ContentCalendar from "@/components/ContentCalendar";
import HashtagStrategy from "@/components/HashtagStrategy";
```

**Edit 2 — Extend tab navigation**: Find the existing tab list in the component. Add 3 new tab buttons after the last existing tab:
- "Compare" tab — icon: two overlapping circles or comparison icon (use SVG or emoji-free text)
- "Calendar" tab — icon: calendar grid
- "Hashtags" tab — icon: hashtag symbol

Tab button styling: match existing tab button styles in the file. Active tab: gradient accent classes. Inactive: `text-gray-400 hover:text-white`.

**Edit 3 — Add tab content panels**: In the tab content switch/conditional, add 3 new cases rendering the new components with the correct props:
- Compare tab: `<CompetitorComparison platform={report.platform ?? "instagram"} currentUsername={report.username} currentReport={report} />`
  - Note: AnalysisReport doesn't have `platform` field — add `platform?: string` as an optional prop passed down from the analyze page, or use a fallback "instagram"
- Calendar tab: `<ContentCalendar platform={...} username={report.username} report={report} />`
- Hashtags tab: `<HashtagStrategy platform={...} username={report.username} report={report} />`

If AnalysisReport doesn't include platform, add `platform?: string` to ReportDashboard's Props interface and pass it from the analyze page. Check src/app/analyze/page.tsx to see if platform is available and pass it down.

Preserve all existing tab content and component logic unchanged.
  </action>
  <verify>
    <automated>cd /Users/Mahesh.Sangawar/personal/anardam/InstaAnalyse && npx tsc --noEmit 2>&1 | head -20 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CompetitorComparison\|ContentCalendar\|HashtagStrategy" src/components/ReportDashboard.tsx` returns 6+ lines (3 imports + 3 usages)
    - `grep "Compare\|Calendar\|Hashtag" src/components/ReportDashboard.tsx` returns 3+ lines (tab labels)
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>3 new components wired into ReportDashboard as tabs. All existing tabs unaffected. TypeScript and ESLint clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run lint` exits 0
- All 3 new component files exist in src/components/
- ReportDashboard imports and renders all 3 new components
</verification>

<success_criteria>
All Phase 4 AI features surfaced in UI. ReportDashboard has Compare, Calendar, and Hashtags tabs. Components fetch from authenticated API routes and render structured data.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-enhancement/04-06-SUMMARY.md` with:
- Components created (3)
- Tab wiring details
- Any adaptations to ReportDashboard Props interface
- Note: compare pre-fills current profile as first handle
</output>
