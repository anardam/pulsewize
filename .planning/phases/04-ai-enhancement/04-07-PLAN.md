---
phase: 04-ai-enhancement
plan: "07"
type: execute
wave: 4
depends_on:
  - "04-06"
files_modified: []
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04
  - AI-05
  - AI-06

must_haves:
  truths:
    - "Pro user sees a report visibly richer than single-agent equivalent"
    - "Free user receives single-agent analysis without errors"
    - "Same handle analyzed twice within 1 hour returns immediately with cached:true"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 4 AI Enhancement feature set. Confirms multi-agent analysis works for Pro, single-agent for Free, and cache dedup functions correctly.

Purpose: Quality gate before marking Phase 4 complete. These behaviors cannot be fully verified by automated tests (subjective quality, timing-dependent cache).
Output: Human sign-off that all Phase 4 success criteria are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ai-enhancement/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 4 AI Enhancement:
- Multi-agent analysis (3 AI providers via OpenRouter → Claude Sonnet synthesis) for Pro users
- Single-agent analysis (existing Claude path) for Free users — unchanged behavior
- 1-hour result cache in Supabase reports table
- Competitor comparison (POST /api/compare) with side-by-side metrics table
- Content calendar (POST /api/calendar) with 30-day weekly grid
- Hashtag strategy (POST /api/hashtags) with 4 categories + avoid list
- All 3 features accessible as tabs in ReportDashboard
  </what-built>
  <how-to-verify>
**Setup:** Ensure OPENROUTER_API_KEY is set in your .env.local before testing Pro analysis.

**Test 1 — Multi-agent vs single-agent (AI-01, AI-02):**
1. Log in as a Free user. Run an analysis on any Instagram profile.
2. Verify: Report appears normally, no errors. Check browser DevTools Network tab — /api/analyze response should NOT contain `multiAgentMeta`.
3. Log in as a Pro user (or update subscription status to 'active' in Supabase). Run analysis on same profile.
4. Verify: Report appears. Response may contain `multiAgentMeta.successCount` indicating 2-3 providers succeeded.
5. Compare the two reports — Pro report should have more specific recommendations.

**Test 2 — Cache dedup (AI-06):**
1. As Pro user, analyze `@nike` on Instagram.
2. Note the response time (should be 15-30s for fresh analysis).
3. Immediately run the same analysis again: `@nike` on Instagram.
4. Verify: Second request returns in under 2 seconds. Check Network response for `"cached": true`.

**Test 3 — Competitor comparison (AI-03):**
1. After analyzing a profile, click the "Compare" tab in the report.
2. Enter 1-2 competitor handles in the same platform.
3. Click Compare.
4. Verify: Side-by-side metrics table appears with correct follower counts and other metrics. Leader column highlighted with purple accent. Narrative paragraph below table. Opportunities list present.

**Test 4 — Content calendar (AI-04):**
1. Click the "Calendar" tab in the report.
2. Click "Generate 30-Day Content Calendar".
3. Verify: Weekly grid appears with 4 weeks (Mon-Sun columns). Each cell has a content idea and posting time. Click a cell to see full details (caption draft, hashtags, media suggestion).

**Test 5 — Hashtag strategy (AI-05):**
1. Click the "Hashtags" tab in the report.
2. Click "Get Hashtag Strategy".
3. Verify: 4 category cards appear (ultra-niche, niche, mid-tier, broad). Each has 8-10 hashtag chips. Avoid list at bottom in red. Caption mix formula visible.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass. Describe any failures to create gap closure tasks.</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all 5 test scenarios.
</verification>

<success_criteria>
All Phase 4 ROADMAP success criteria verified:
1. Pro user receives multi-agent synthesized report (richer than free tier)
2. Free user receives single-agent analysis (no degraded experience)
3. Competitor comparison works end-to-end
4. Content calendar generates 30-day grid
5. Hashtag strategy shows niche vs oversaturated categories
6. Cache dedup returns in under 2 seconds on second request
</success_criteria>

<output>
After verification, create `.planning/phases/04-ai-enhancement/04-07-SUMMARY.md` with:
- Verification results (pass/fail per test)
- Any issues found
- Phase 4 completion status
</output>
