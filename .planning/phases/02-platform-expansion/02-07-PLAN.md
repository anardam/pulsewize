---
phase: 02-platform-expansion
plan: "07"
type: execute
wave: 5
depends_on:
  - "02-06"
files_modified: []
autonomous: false
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
  - PLAT-05
  - PLAT-06
  - PLAT-07
must_haves:
  truths:
    - "User can navigate to /analyze and see the 6-platform card grid"
    - "Clicking YouTube and entering a handle produces a complete AI analysis report"
    - "Clicking Twitter/X and entering a handle produces a complete AI analysis report"
    - "Clicking TikTok and entering a handle produces a complete AI analysis report"
    - "Selecting LinkedIn or Facebook and entering a handle that fails scraping shows error + retry (no manual entry form)"
    - "Platform health badges are visible on cards"
    - "Selecting Instagram still works as before (no regression)"
    - "Refreshing the page returns to the last used platform selection (localStorage persistence)"
  artifacts:
    - path: src/app/(dashboard)/analyze/page.tsx
      provides: "2-step analyze flow live in browser"
    - path: src/components/PlatformGrid.tsx
      provides: "Platform grid with health badges visible"
  key_links:
    - from: "/analyze page"
      to: "/api/analyze"
      via: "POST with platform field set to selected platform"
      pattern: "platform.*POST body"
    - from: "/analyze page"
      to: "/api/platform-health"
      via: "fetch on mount in PlatformGrid"
      pattern: "health badges"
---

<objective>
Human verification of the complete Phase 2 platform expansion.

Purpose: Confirm all 5 new platforms are reachable through the analyze flow and that the health indicator, localStorage persistence, and error states work correctly in a real browser.
Output: User confirms Phase 2 is working or provides specific issues to fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-expansion/02-CONTEXT.md
@.planning/phases/02-platform-expansion/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and confirm TypeScript + lint pass</name>
  <files></files>
  <read_first>
    - package.json — confirm dev and lint scripts
  </read_first>
  <action>
Run the following checks before handing off for human verification:

```bash
npx tsc --noEmit
```

If TypeScript exits 0, proceed. If there are errors, fix them before proceeding to the checkpoint.

```bash
npm run lint 2>&1 | head -50
```

Address any lint errors introduced by Phase 2 code (no-console violations, unused imports).

Then start the dev server:
```bash
npm run dev
```

Confirm the server starts without errors on http://localhost:3000.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -10 && grep -n "requiresManualEntry: true" src/lib/scrapers/instagram/cascade.ts</automated>
  </verify>
  <done>TypeScript exits 0. Lint passes (or only pre-existing warnings). Instagram cascade still has requiresManualEntry: true (no regression). Dev server starts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 platform expansion:
    - 5 new platform scrapers (YouTube, Twitter/X, TikTok, LinkedIn, Facebook) registered in the scraper registry
    - Platform card grid on /analyze page replacing the single-step Instagram input
    - Per-platform AI prompts customized for each platform's growth strategy
    - Platform health monitoring with Vercel Cron canary + GET /api/platform-health endpoint
    - Error + retry UX for non-Instagram scraping failures (no manual entry form)
    - localStorage persistence of last selected platform
  </what-built>
  <how-to-verify>
    Visit http://localhost:3000/analyze (sign in if needed).

    **Step 1 — Platform grid:**
    - You should see 6 platform cards in a grid (Instagram, YouTube, Twitter/X, TikTok, LinkedIn, Facebook)
    - Each card should have a colored circle icon and health badge (dot)
    - On mobile width (resize browser to ~375px): grid should show 2 columns

    **Step 2 — Platform selection + localStorage:**
    - Click "YouTube"
    - You should see a handle input with "YouTube username" placeholder
    - There should be a "← Back" button to return to the grid
    - Refresh the page — you should land back on the grid with YouTube pre-selected (localStorage)

    **Step 3 — YouTube analysis (if YOUTUBE_API_KEY is configured):**
    - With YouTube selected, enter handle: `mkbhd`
    - Click Analyze — should produce an AI report (check that it mentions "video" strategy, not Instagram)
    - If YOUTUBE_API_KEY is not yet configured, skip this and note it

    **Step 4 — Twitter/X analysis (if SCRAPECREATORS_API_KEY is configured):**
    - Click Twitter/X card, enter handle: `elonmusk`
    - Should produce an AI report mentioning threads/engagement strategy

    **Step 5 — Error + retry UX:**
    - Click LinkedIn card, enter handle: `nonexistentuserxyz123abc`
    - Should show "Unable to fetch profile. Try again later." with a "Try again" button
    - Should NOT show a manual entry form

    **Step 6 — Instagram regression check:**
    - Click Instagram card, enter a real Instagram handle
    - Should work exactly as before Phase 2

    **Step 7 — Health badges:**
    - Open DevTools Network tab
    - Reload /analyze
    - Confirm a request to /api/platform-health is made and returns 200 with 6 platform statuses
  </how-to-verify>
  <resume-signal>
    Type "approved" if all steps pass.
    If issues found, describe each: e.g., "YouTube card missing", "Health badges not showing", "Error on Twitter submit".
    Claude will fix and re-present for verification.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirmation that the 2-step analyze flow works end-to-end across all 6 platforms.
</verification>

<success_criteria>
All Phase 2 ROADMAP success criteria confirmed by human:
1. User can select YouTube, Twitter/X, or TikTok and receive a complete AI analysis report
2. User can select LinkedIn or Facebook and receive an error + retry (no manual entry regression)
3. Every platform returns normalized data through the same analysis pipeline
4. Platform health indicator is visible on cards before starting analysis
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-expansion/02-07-SUMMARY.md`
</output>
