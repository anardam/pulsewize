---
phase: 01-foundation
plan: "06"
type: execute
wave: 5
depends_on: ["01-05"]
files_modified: []
autonomous: false
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
must_haves:
  truths:
    - "User can sign up with email and password and receives verification email"
    - "User can sign in with Google OAuth and land on /dashboard"
    - "User session persists across browser refresh"
    - "User can log out from /dashboard and is redirected to /"
    - "Unauthenticated user trying to visit /dashboard is redirected to /login"
    - "Authenticated user visiting /login is redirected to /dashboard"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page"
    - path: "proxy.ts"
      provides: "Auth redirect enforcement"
  key_links:
    - from: "proxy.ts"
      to: "Supabase auth cookie"
      via: "getUser() server validation"
      pattern: "getUser"
---

<objective>
Human verification of the complete Phase 1 authentication and navigation flow. Claude cannot verify email delivery, Google OAuth redirect, or visual correctness. This checkpoint confirms the entire phase works from a user's perspective.

Purpose: AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07 — these requirements have manual-only verification per VALIDATION.md.

Output: Confirmation that Phase 1 is complete and working end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1: Foundation — auth system, 7-page SaaS app, scraper registry, atomic usage enforcement, @tesserix/web UI.
  </what-built>
  <how-to-verify>
    Run the dev server: `npm run dev`

    **Test 1 — Landing page (unauthenticated):**
    1. Visit http://localhost:3000
    2. Confirm: Hero section visible, "Get started" and "See how it works" buttons present
    3. Confirm: Features and pricing sections visible
    4. Confirm: Navbar shows "Log in" and "Get started" (no user menu)

    **Test 2 — Signup with email/password (AUTH-01 + AUTH-02):**
    1. Click "Get started" → should reach /signup
    2. Fill email + password (8+ chars) → click "Create account"
    3. Confirm: Success message "Check your inbox to confirm your email." shown
    4. Confirm: Verification email arrives in inbox (required for AUTH-02)

    **Test 3 — Email/password login (AUTH-01):**
    1. After verifying email, visit http://localhost:3000/login
    2. Log in with the email/password from Test 2
    3. Confirm: Redirected to /dashboard
    4. Confirm: Dashboard shows "No analyses yet" empty state

    **Test 4 — Session persistence (AUTH-04):**
    1. While logged in on /dashboard, refresh the page (Cmd/Ctrl+R)
    2. Confirm: Still on /dashboard, still logged in (not redirected to /login)

    **Test 5 — Protected route redirect:**
    1. Open a new incognito window → visit http://localhost:3000/dashboard
    2. Confirm: Redirected to /login (proxy.ts working)

    **Test 6 — Google OAuth (AUTH-03):**
    1. Visit /login → click "Continue with Google"
    2. Complete Google consent screen
    3. Confirm: Redirected to /dashboard after sign-in

    **Test 7 — Log out (AUTH-06):**
    1. On any authenticated page, click the user avatar (top right)
    2. Click "Log out"
    3. Confirm: Redirected to /
    4. Visit /dashboard again → confirm redirected to /login

    **Test 8 — Password reset (AUTH-05):**
    (Phase 1 basic check: Supabase sends the email)
    1. Visit /login → attempt to trigger password reset flow
    2. (If a "Forgot password?" link was added in AuthForm, use it; otherwise call `supabase.auth.resetPasswordForEmail(email)` directly in browser console)
    3. Confirm: Reset email received

    **Test 9 — Usage limit (INFRA-03):**
    1. Log in and visit /analyze
    2. Run analysis for a real Instagram username (e.g., "instagram")
    3. Confirm: Analysis runs and report is displayed
    4. (Optional) Run 3 analyses total
    5. Confirm: 4th attempt shows "You've used all 3 free analyses this month" error

    **Test 10 — Visual check:**
    1. Check all pages load with dark theme (#0a0a0f background)
    2. Check violet accent color (#a855f7) on gradient buttons and active nav items
    3. Check navigation links: Dashboard → /dashboard, Analyze → /analyze, Reports → /reports

    **If anything fails**, describe the failure and the step it occurred on. Claude will fix and resume.
  </how-to-verify>
  <resume-signal>Type "approved" when all tests pass, or describe what failed and at which step.</resume-signal>
</task>

</tasks>

<verification>
All 10 tests pass per user confirmation.
</verification>

<success_criteria>
- User types "approved"
- Auth flows work end-to-end
- Session persistence confirmed
- Route protection confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
