---
phase: 01-foundation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - src/__tests__/.gitkeep
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running `npx vitest run` exits 0 with no test failures"
    - "vitest.config.ts exists with @/ path alias matching tsconfig"
    - "src/__tests__/ directory exists and is committed"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration with path aliases"
    - path: "src/__tests__/.gitkeep"
      provides: "Test directory scaffold"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "@/ alias"
      pattern: "resolve.*alias.*@"
---

<objective>
Install vitest and create the test infrastructure scaffold. Every subsequent plan's `<verify>` commands depend on `npx vitest run` being available and working with the project's path aliases.

Purpose: Nyquist compliance — per VALIDATION.md, all automated verify commands run `npx vitest run`. Without this Wave 0 plan, every downstream plan's verify step would fail with "vitest: command not found".

Output: vitest installed as devDependency, `vitest.config.ts` configured with `@/` alias, `src/__tests__/` directory committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and create vitest.config.ts</name>
  <read_first>
    - /Users/Mahesh.Sangawar/personal/anardam/SocialLens/package.json
    - /Users/Mahesh.Sangawar/personal/anardam/SocialLens/tsconfig.json
  </read_first>
  <files>
    package.json,
    vitest.config.ts,
    src/__tests__/.gitkeep
  </files>
  <action>
1. Install devDependencies (do NOT upgrade other packages yet — that is Plan 01):

```bash
npm install -D vitest@^4.1.2 @vitejs/plugin-react@^4.0.0 @testing-library/react@^16.3.2 @testing-library/jest-dom@^6.6.0 @types/testing-library__jest-dom jsdom@^26.0.0
```

2. Create `vitest.config.ts` at project root:

```typescript
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
import path from "path";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: [],
    include: ["src/**/*.test.{ts,tsx}", "src/__tests__/**/*.test.{ts,tsx}"],
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
});
```

3. Create `src/__tests__/.gitkeep` (empty file to commit the directory).

4. Do NOT run `npm run build` or change any other files in this plan.
  </action>
  <verify>
    <automated>cd /Users/Mahesh.Sangawar/personal/anardam/SocialLens && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists at project root: `ls vitest.config.ts`
    - vitest is listed in devDependencies in package.json: `grep '"vitest"' package.json`
    - `@/` alias resolves to `./src`: grep for `resolve.*@` in vitest.config.ts
    - `npx vitest run` exits without "vitest: command not found" error (0 tests passing is acceptable)
    - `src/__tests__/.gitkeep` exists: `ls src/__tests__/`
  </acceptance_criteria>
  <done>vitest runs without error. No test files need to exist yet — 0 passed, 0 failed is the correct output for an empty test suite.</done>
</task>

</tasks>

<verification>
`npx vitest run --reporter=verbose` exits 0 with output containing "Test Files" line (even if 0 files).
</verification>

<success_criteria>
- vitest installed and runnable
- Path alias @/ works in test files
- Test directory structure committed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md`
</output>
